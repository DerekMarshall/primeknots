# Build output
/build/
/build-*/

# Emitted visualization data (regenerated by `at emit`)
/viz/data/

# Local editor / tool state
/.claude/settings.local.json
/.spyc-context-*.json
.DS_Store

# Common C/C++ artifacts (out-of-tree builds should keep these empty, but just in case)
*.o
*.obj
*.a
*.so
*.dylib
*.exe
